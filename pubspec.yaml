name: todo
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.0.5
  table_calendar: ^3.0.8
  flutter_native_splash: ^2.2.17
  flutter_slidable: ^2.0.0
  firebase_core: ^2.4.1
  cloud_firestore: ^4.3.1
  shared_preferences: ^2.0.17
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  generate: true
  uses-material-design: true

  assets:
     - assets/

flutter_native_splash:
  color: "#DFECDB"
  image: assets/3.0x/splash@3x.png